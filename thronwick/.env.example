# Optional override for the public R2 model origin used in local development and builds.
# The application defaults to this validated endpoint when the variable is absent.
VITE_ASSET_BASE_URL=https://pub-37dce3d7ecf94df9acc08cadeb70022c.r2.dev/
