[build]
  command = "npm run build"
  publish = "dist"

# This is required for React/Vite Single Page Applications
# It routes all requests to index.html so client-side routing works.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200